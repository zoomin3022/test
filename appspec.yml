version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/test

permissions:
  - object: /home/ubuntu/test/
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: "755"

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu